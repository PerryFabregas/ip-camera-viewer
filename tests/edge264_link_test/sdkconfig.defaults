# ESP32-P4 + PSRAM (edge264 alloue ses buffers via les callbacks PSRAM en usage
# réel ; ici on ne fait que builder/linker).
CONFIG_SPIRAM=y
CONFIG_IDF_EXPERIMENTAL_FEATURES=y
CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192
